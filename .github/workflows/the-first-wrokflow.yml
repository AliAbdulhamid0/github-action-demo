name: First Test Action

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello 👋 GitHub Actions is working!"

      - name: Show date
        run: date

      - name: List files
        run: ls -la
