name: test echo message 


on: 
  push: 
    branches:
      - main 

jobs:
  The_first_job_for_me: 
    runs-on: ubuntu-latest 

    steps:
      - name: lets echo a message
        run: echo "kolo fol ISA" 

      - name: checkout my code 
        uses: actions/checkout@v6.0.1

      - name: list all file included the hidden ones 
        run: ls -la 
